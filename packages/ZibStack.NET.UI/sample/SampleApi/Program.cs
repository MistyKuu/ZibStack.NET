using Scalar.AspNetCore;
using SampleApi;
using SampleApi.Models;
using ZibStack.NET.UI;

var builder = WebApplication.CreateBuilder(args);
builder.Services.AddOpenApi();

var app = builder.Build();

app.UseStaticFiles();
app.MapOpenApi();
app.MapScalarApiReference();

// CRUD endpoints NOT mapped — this is a read-only playground demo.
// In a real app you'd add:
//   builder.Services.AddDbContext<AppDbContext>(o => o.UseSqlite("Data Source=app.db"));
//   builder.Services.AddAppDbContextCrudStores();
//   app.MapProductEndpoints();

// One-liner: registers GET /api/forms/{name} + /api/tables/{name} for every
// [UiForm]/[UiTable]/[ImTiredOfCrud] type in the assembly (generated by ZibStack.NET.UI).
app.MapZibStackUiSchemas();

// Live playground: POST C# code → compile with generators → return schemas
app.MapPlayground();

app.Run();
