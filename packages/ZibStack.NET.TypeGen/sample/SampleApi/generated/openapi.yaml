# @generated by ZibStack.NET.TypeGen — do not edit
openapi: 3.0.3
info:
  title: Sample Order API
  version: 1.2.3
  description: Demo service exercising the TypeGen fluent configurator.
paths:
  /api/orders:
    get:
      tags: [Order]
      operationId: listOrder
      parameters:
        - name: page
          in: query
          description: Page number (1-based).
          schema:
            type: integer
            format: int32
        - name: pageSize
          in: query
          description: Items per page.
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PaginatedResponseOfOrder' }
    post:
      tags: [Order]
      operationId: createOrder
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateOrderRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Order' }
  /api/orders/{id}:
    get:
      tags: [Order]
      operationId: getOrderById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Order' }
        '404':
          description: Not Found
    patch:
      tags: [Order]
      operationId: updateOrder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateOrderRequest' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Order' }
    delete:
      tags: [Order]
      operationId: deleteOrder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: No Content
components:
  schemas:
    Order:
      type: object
      required:
        - Id
        - Customer
        - Total
        - Status
        - Items
      properties:
        Id:
          type: integer
          format: int32
        Customer:
          type: string
        Total:
          type: number
          format: double
        Status:
          $ref: '#/components/schemas/OrderStatus'
        CreditCardMasked:
          type: string
          nullable: true
          format: password
          description: Last four digits only.
        Items:
          type: array
          items:
            $ref: '#/components/schemas/OrderItem'
    OrderItem:
      type: object
      required:
        - Sku
        - Quantity
        - UnitPrice
      properties:
        Sku:
          type: string
        Quantity:
          type: integer
          format: int32
        UnitPrice:
          type: number
          format: double
    CustomerV1:
      type: object
      required:
        - Id
        - Name
        - Email
      properties:
        Id:
          type: integer
          format: int32
        Name:
          type: string
          description: Display name shown in receipts.
        Email:
          type: string
          format: email
          description: Verified contact email.
    OrderStatus:
      type: string
      enum:
        - Pending
        - Shipped
        - Delivered
        - Cancelled
    PaginatedResponseOfOrder:
      type: object
      required: [items, totalCount, page, pageSize, totalPages, hasNextPage, hasPreviousPage]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/Order' }
        totalCount: { type: integer, format: int32 }
        page: { type: integer, format: int32 }
        pageSize: { type: integer, format: int32 }
        totalPages: { type: integer, format: int32 }
        hasNextPage: { type: boolean }
        hasPreviousPage: { type: boolean }
