# @generated by ZibStack.NET.TypeGen — do not edit
openapi: 3.0.3
info:
  title: Sample Order API
  version: 1.2.3
  description: Demo service exercising the TypeGen fluent configurator.
paths: {}
components:
  schemas:
    Order:
      type: object
      required:
        - Id
        - Customer
        - Total
        - Status
        - Items
      properties:
        Id:
          type: integer
          format: int32
        Customer:
          type: string
        Total:
          type: number
          format: double
        Status:
          $ref: '#/components/schemas/OrderStatus'
        CreditCardMasked:
          type: string
          nullable: true
          format: password
          description: Last four digits only.
        Items:
          type: array
          items:
            $ref: '#/components/schemas/OrderItem'
    OrderItem:
      type: object
      required:
        - Sku
        - Quantity
        - UnitPrice
      properties:
        Sku:
          type: string
        Quantity:
          type: integer
          format: int32
        UnitPrice:
          type: number
          format: double
    CustomerV1:
      type: object
      required:
        - Id
        - Name
        - Email
      properties:
        Id:
          type: integer
          format: int32
        Name:
          type: string
        Email:
          type: string
    OrderStatus:
      type: string
      enum:
        - Pending
        - Shipped
        - Delivered
        - Cancelled
