# @generated by ZibStack.NET.TypeGen — do not edit
openapi: 3.0.3
info:
  title: ZibStack Sample API
  version: 1.0.0
  description: Full CRUD + schema + TS client contract, one attribute pass.
paths:
  /api/players:
    get:
      tags: [Player]
      operationId: listPlayer
      parameters:
        - name: page
          in: query
          description: Page number (1-based).
          schema:
            type: integer
            format: int32
        - name: pageSize
          in: query
          description: Items per page.
          schema:
            type: integer
            format: int32
        - name: filter
          in: query
          description: Filter DSL — 'Field>value,Field2=*text'.
          schema:
            type: string
        - name: sort
          in: query
          description: Sort DSL — '-Field,Field2' (prefix '-' for desc).
          schema:
            type: string
        - name: select
          in: query
          description: Projection — 'Field1,Field2'.
          schema:
            type: string
        - name: count
          in: query
          description: "When true, returns only { count: int }."
          schema:
            type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PaginatedResponseOfPlayer' }
    post:
      tags: [Player]
      operationId: createPlayer
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreatePlayerRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Player' }
  /api/players/{id}:
    get:
      tags: [Player]
      operationId: getPlayerById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Player' }
        '404':
          description: Not Found
    patch:
      tags: [Player]
      operationId: updatePlayer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdatePlayerRequest' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Player' }
    delete:
      tags: [Player]
      operationId: deletePlayer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: No Content
  /api/players/bulk:
    post:
      tags: [Player]
      operationId: bulkCreatePlayer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items: { $ref: '#/components/schemas/CreatePlayerRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/Player' }
  /api/players/bulk-delete:
    post:
      tags: [Player]
      operationId: bulkDeletePlayer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items: { type: integer, format: int32 }
      responses:
        '204':
          description: No Content
  /api/teams:
    get:
      tags: [TeamResponse]
      operationId: listTeamResponse
      parameters:
        - name: page
          in: query
          description: Page number (1-based).
          schema:
            type: integer
            format: int32
        - name: pageSize
          in: query
          description: Items per page.
          schema:
            type: integer
            format: int32
        - name: filter
          in: query
          description: Filter DSL — 'Field>value,Field2=*text'.
          schema:
            type: string
        - name: sort
          in: query
          description: Sort DSL — '-Field,Field2' (prefix '-' for desc).
          schema:
            type: string
        - name: select
          in: query
          description: Projection — 'Field1,Field2'.
          schema:
            type: string
        - name: count
          in: query
          description: "When true, returns only { count: int }."
          schema:
            type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PaginatedResponseOfTeamResponse' }
    post:
      tags: [TeamResponse]
      operationId: createTeamResponse
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateTeamRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TeamResponse' }
  /api/teams/{id}:
    get:
      tags: [TeamResponse]
      operationId: getTeamResponseById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TeamResponse' }
        '404':
          description: Not Found
    patch:
      tags: [TeamResponse]
      operationId: updateTeamResponse
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateTeamRequest' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/TeamResponse' }
    delete:
      tags: [TeamResponse]
      operationId: deleteTeamResponse
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int32
      responses:
        '204':
          description: No Content
components:
  schemas:
    Player:
      type: object
      required:
        - Id
        - Name
        - Level
        - Salary
        - CreatedAt
        - UpdatedAt
      properties:
        Id:
          type: integer
          format: int32
        Name:
          type: string
          minLength: 2
          maxLength: 50
        Level:
          type: integer
          format: int32
          minimum: 1
          maximum: 100
        Email:
          type: string
          nullable: true
          format: email
          description: Contact email (optional).
        Salary:
          type: string
          format: decimal
          description: Encoded as string to preserve precision.
        Bio:
          type: string
          nullable: true
          format: textarea
        TeamId:
          type: integer
          format: int32
          nullable: true
        Team:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/TeamResponse'
        CreatedAt:
          type: string
          format: date-time
        UpdatedAt:
          type: string
          format: date-time
    TeamResponse:
      type: object
      required:
        - Id
        - Name
        - MaxMembers
        - Players
        - CreatedAt
        - UpdatedAt
      properties:
        Id:
          type: integer
          format: int32
        Name:
          type: string
          maxLength: 100
        Description:
          type: string
          nullable: true
          maxLength: 500
        MaxMembers:
          type: integer
          format: int32
          minimum: 1
          maximum: 100
        Players:
          type: array
          items:
            $ref: '#/components/schemas/Player'
        CreatedAt:
          type: string
          format: date-time
        UpdatedAt:
          type: string
          format: date-time
    PaginatedResponseOfPlayer:
      type: object
      required: [items, totalCount, page, pageSize, totalPages, hasNextPage, hasPreviousPage]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/Player' }
        totalCount: { type: integer, format: int32 }
        page: { type: integer, format: int32 }
        pageSize: { type: integer, format: int32 }
        totalPages: { type: integer, format: int32 }
        hasNextPage: { type: boolean }
        hasPreviousPage: { type: boolean }
    PaginatedResponseOfTeamResponse:
      type: object
      required: [items, totalCount, page, pageSize, totalPages, hasNextPage, hasPreviousPage]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/TeamResponse' }
        totalCount: { type: integer, format: int32 }
        page: { type: integer, format: int32 }
        pageSize: { type: integer, format: int32 }
        totalPages: { type: integer, format: int32 }
        hasNextPage: { type: boolean }
        hasPreviousPage: { type: boolean }
