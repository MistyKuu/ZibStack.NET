<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles>
    <InterceptorsPreviewNamespaces>$(InterceptorsPreviewNamespaces);ZibStack.Generated</InterceptorsPreviewNamespaces>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.0.0" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <!-- Brings in generator + analyzers + code fixes + abstractions in one go.
       Intentional bad-shape fixtures (e.g. static method with [Record] for the AOP0001
       ground-truth test) are pragma-suppressed at the call site. -->
  <Import Project="..\..\src\ZibStack.NET.Aop.All.props" />

  <ItemGroup>
    <ProjectReference Include="..\..\..\ZibStack.NET.Log\src\ZibStack.NET.Log.Abstractions\ZibStack.NET.Log.Abstractions.csproj" />
    <!-- Result integration fixtures — methods returning Result<T> get aspect failures
         converted to failed Results by the generated interceptors. -->
    <ProjectReference Include="..\..\..\ZibStack.NET.Result\src\ZibStack.NET.Result\ZibStack.NET.Result.csproj" />
    <!-- Optional add-on packages — exercised by behavioral fixtures so we'd notice
         if their handlers ever stop being intercepted by the main AOP generator. -->
    <ProjectReference Include="..\..\src\ZibStack.NET.Aop.Polly\ZibStack.NET.Aop.Polly.csproj" />
    <ProjectReference Include="..\..\src\ZibStack.NET.Aop.HybridCache\ZibStack.NET.Aop.HybridCache.csproj" />
  </ItemGroup>

</Project>
